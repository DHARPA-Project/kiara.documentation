channels:
  - conda-forge
  - dharpa

host_requirements:
  - pip
  - python
  - setuptools<=63
  - setuptools_scm

test:
  imports:
    - kiara_plugin.documentation
  source_files:
    - tests
    - examples
  commands:
    - kiara module list
